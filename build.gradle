plugins {
    id 'java'
}

group 'com.yo.prototype'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {

    compile 'org.springframework.cloud:spring-cloud-stream-binder-rabbit:3.0.8.RELEASE'

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
